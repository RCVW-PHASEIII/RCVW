PROJECT ( MapPlugin VERSION 4.0.0 LANGUAGES CXX )

SET (TMX_PLUGIN_NAME "MAP")

ADD_TMX_PLUGIN ()

TARGET_INCLUDE_DIRECTORIES ( ${PROJECT_NAME} PUBLIC )
TARGET_LINK_LIBRARIES ( ${PROJECT_NAME} PUBLIC libtmx-j2735 )
