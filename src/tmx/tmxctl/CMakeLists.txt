PROJECT (tmxctl VERSION 4.0.0 LANGUAGES CXX)

SET (${PROJECT_NAME}_NO_PLUGIN_PACKAGE ON)

ADD_TMX_PLUGIN()

INSTALL (TARGETS ${PROJECT_NAME}
         DESTINATION bin
         COMPONENT ${PROJECT_NAME})
